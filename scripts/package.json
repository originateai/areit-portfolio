{
  "name": "areit-scripts",
  "version": "1.0.0",
  "private": true,
  "description": "Local CLI scripts for the areit value layer (run on James's machine, not bundled by Netlify).",
  "type": "commonjs",
  "scripts": {
    "export-model": "node export-model.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "pdf-parse": "^1.1.1",
    "xlsx": "^0.18.5"
  }
}
